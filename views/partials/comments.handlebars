<div class="comments">
  {{#each this}}

  <section class="comment border-bottom border-2 border-light mt-3 pb-3">
    <div class="meta">
      @{{user.username}} ∙ <span class='fst-italic fw-light fs-7'>{{format_date created_at}}</span>
    </div>
    <div class="comments">
      {{comment_text}}
    </div>
  </section>
  
  {{/each}}
</div>